{
  "name": "my-app",
  "version": "0.0.1",
  "description": "An app for restarting server on timeout",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "robin.sotoh",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0"
  }
}
